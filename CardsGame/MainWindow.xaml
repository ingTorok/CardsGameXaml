<Window x:Class="CardsGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardsGame"
        mc:Ignorable="d"
        MinHeight="300"
        MinWidth="500"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Label x:Name="LabelCountDown" Content="Remain: 00:55" Padding="10" Margin="10" HorizontalAlignment="Left"/>
            <Label x:Name="LabelPonits" Content="Points: 100" Padding="10" Margin="10" HorizontalAlignment="Left"/>
            <Label x:Name="LabelReaction" Content="Reaction: 127" Padding="10" Margin="10" HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Footer -->
        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ButtonNo"        Grid.Row="0" Content="No"        Grid.Column="0" Padding="10" Margin="10" MaxHeight="60" MaxWidth="120"/>
            <Button x:Name="ButtonPartially" Grid.Row="0" Content="Pattially" Grid.Column="1" Padding="10" Margin="10" MaxHeight="60" MaxWidth="120"/>
            <Button x:Name="ButtonYes"       Grid.Row="0" Content="Yes"       Grid.Column="2" Padding="10" Margin="10" MaxHeight="60" MaxWidth="120"/>
            <Button x:Name="ButtonStart"     Grid.Row="0" Content="Start"     Grid.Column="3" Padding="10" Margin="10" MaxHeight="60" MaxWidth="120"/>
            
        </Grid>
    </Grid>
</Window>
